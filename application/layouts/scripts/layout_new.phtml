<!DOCTYPE HTML>

<html>
<head>
    <meta http-equiv="Content-Type" content="application/xhtml+xml; charset=utf-8"/>
    <title><?php echo $this->translate($this->title); ?></title>
    <?php $this->headLink()->appendStylesheet($this->baseUrl('/css/style_basic.css'));
    $this->headScript()->appendFile($this->baseUrl('/js/jquery-1.7.2.min.js'));
    $this->headScript()->appendFile($this->baseUrl('/js/bootstrap.min.js'));
    $this->headScript()->appendFile($this->baseUrl('/js/massidea.js'));

    echo $this->headLink();
    echo $this->headMeta();
    echo $this->headScript();

    ?>
</head>
<body>
<div class="container">
    <header>
        <a id="headerLogo" href="<?php echo $this->url(array('language' => $this->language),
            'lang_default', true); ?>/"><img src="/img/massideaLogo.png"/></a>
        <img id="headerSignet" src="/img/signet.png"/>
        <?php
        if (!$this->authenticated) : ?>
            <?php echo $this->loginform; ?>
            <?php else : ?>
            <div id="headerLoggedIn">
                <?php echo $this->username; ?>
                <span class="grey"> |</span>
                <a href="<?php echo $this->url(array(
                    'controller' => 'account',
                    'action' => 'logout',
                    'language' => $this->language,
                ), 'lang_default', true); ?>">
                    <?php echo $this->translate('index-logout'); ?>
                </a>
            </div>
            <?php endif; ?>
    </header>
    <section id="content">
        <?php echo $this->layout()->content; ?>
    </section>


    <footer>
     Here will be Lisa´s cool and sophisticated footer :)

    </footer>
</div>
</body>
</html>


